#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Solution {
public:

	void canReach(int threshold, int rows, int cols, int* flag, int currow, int curcol) {
		if(currow < 0 || currow >= rows)

	}
    int movingCount(int threshold, int rows, int cols)
    {
 		if(thresholde == 0 || rows == 0 || cols == 0)
 			return 0;
 		int *flag = new int[rows*cols];
 		canReach(thresholde, rows, cols, flag, 0, 0);

    }
};
int main(void) {
	return 0;
}